import React from 'react';
import Uploader from '../../../elements/Uploader/Uploader';


const NewIcon = () => (
  <React.Fragment>
    <Uploader link="icon" updateImage={() => {}} />
  </React.Fragment>
);

export default NewIcon;
